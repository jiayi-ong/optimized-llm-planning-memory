# configs/logging/default.yaml
# @package logging
level: INFO
use_tensorboard: true
use_wandb: false
wandb_project: optllm-planning
wandb_entity: null
log_every_n_steps: 10
# log_file: null means stdout only. Set to a path to tee logs to a file, e.g.:
#   python scripts/run_episode.py logging.log_file=outputs/logs/run.log
log_file: null
